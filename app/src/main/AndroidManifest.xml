<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dreamteam.focus">

    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/focus_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".client.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".server.BackgroundService"
            android:enabled="true"
            android:exported="true"
            android:stopWithTask="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>

        </service>

        <activity android:name=".client.Profiles">

        </activity>

        <activity android:name=".client.Schedules">

        </activity>

        <activity android:name=".client.AddProfileToSchedule">


        </activity>

        <activity android:name=".client.AddProfileToNewSchedule">


        </activity>

        <activity android:name=".client.CreateSchedule">

        </activity>

        <activity android:name=".client.EditSchedule">

        </activity>



        <activity
            android:name=".client.CreateProfile"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".client.Profiles" />
        <activity
            android:name=".client.EditProfile"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".client.timePicker"></activity>
    </application>

</manifest>